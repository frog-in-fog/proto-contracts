syntax = "proto3";

package auth;

option go_package = "proto-contracts/gen/auth;auth";

service AuthService {
  rpc Login(LoginRequest) returns (LoginResponse);
}

message LoginRequest {
  string username = 1;
  string password = 2;
  string fingetprint = 3;
}

message LoginResponse {
  string token = 1;
}
